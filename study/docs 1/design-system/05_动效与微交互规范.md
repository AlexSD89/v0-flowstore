# 动效与微交互规范 2.1 升级版

**版本**: 2.1.0 | **日期**: 2025-08-12 | **状态**: 拂晓深空融合版  
**基于**: 原动效规范 + 拂晓与深空设计哲学 + 智能化交互体验

---

## 核心设计哲学：拂晓与深空

### 设计理念升级

🌅 **拂晓 (Dawn)** - 希望与创新的象征  
• **视觉表达**: 温暖的渐变、柔和的光芒、新生的力量  
• **应用场景**: CTA按钮、成功状态、激励反馈、突出显示  
• **情感传达**: 乐观、积极、温暖、充满希望

🌌 **深空 (Deep Space)** - 深度与专业的象征  
• **视觉表达**: 深郁的背景、宁静的氛围、广阔的空间  
• **应用场景**: 主体背景、工作区域、深度思考模式  
• **情感传达**: 专业、沉稳、专注、深度

### 融合策略

```css
/* 拂晓与深空核心变量 */
:root {
  /* 深空基底 */
  --deep-space-primary: #05080E;    /* 主体深空背景 */
  --deep-space-secondary: #0A0F1C;  /* 次级深空区域 */
  --deep-space-accent: #12172B;     /* 深空强调色 */
  
  /* 拂晓光芒 */
  --dawn-warm: #FFB366;              /* 温暖拂晓 */
  --dawn-bright: #FF8A4C;            /* 明亮拂晓 */
  --dawn-glow: #FFA366;              /* 拂晓光芒 */
  
  /* Cloudsway紫青主色 */
  --cloudsway-purple: #8B5CF6;       /* 核心紫色 */
  --cloudsway-cyan: #22D3EE;         /* 核心青色 */
  --cloudsway-gradient: linear-gradient(135deg, #8B5CF6 0%, #22D3EE 100%);
}
```

---

## 动效系统基础

### 缓动函数

```css
/* 缓动函数系统 */
:root {
  /* 基础缓动 */
  --ease-linear: linear;
  --ease-in: cubic-bezier(0.4, 0, 1, 1);
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
  --ease-in-out: cubic-bezier(0.4, 0, 0.2, 1);
  
  /* 拂晓缓动 */
  --ease-dawn-warm: cubic-bezier(0.25, 0.46, 0.45, 0.94);
  --ease-dawn-bright: cubic-bezier(0.175, 0.885, 0.32, 1.275);
  --ease-dawn-glow: cubic-bezier(0.68, -0.55, 0.265, 1.55);
  
  /* 深空缓动 */
  --ease-deep-space: cubic-bezier(0.4, 0, 0.2, 1);
  --ease-deep-space-accent: cubic-bezier(0.25, 0.46, 0.45, 0.94);
  
  /* 特殊缓动 */
  --ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55);
  --ease-spring: cubic-bezier(0.175, 0.885, 0.32, 1.275);
  --ease-elastic: cubic-bezier(0.68, -0.55, 0.265, 1.55);
}
```

### 持续时间

```css
/* 持续时间系统 */
:root {
  /* 基础持续时间 */
  --duration-0: 0ms;
  --duration-75: 75ms;
  --duration-100: 100ms;
  --duration-150: 150ms;
  --duration-200: 200ms;
  --duration-300: 300ms;
  --duration-500: 500ms;
  --duration-700: 700ms;
  --duration-1000: 1000ms;
  
  /* 拂晓持续时间 */
  --duration-dawn-fast: 200ms;
  --duration-dawn-normal: 400ms;
  --duration-dawn-slow: 600ms;
  
  /* 深空持续时间 */
  --duration-deep-space-fast: 150ms;
  --duration-deep-space-normal: 300ms;
  --duration-deep-space-slow: 500ms;
}
```

---

## 基础动效

### 淡入淡出

```css
/* 淡入淡出动效 */
.fade-in {
  animation: fadeIn var(--duration-dawn-normal) var(--ease-dawn-warm);
}

.fade-out {
  animation: fadeOut var(--duration-deep-space-fast) var(--ease-deep-space);
}

@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes fadeOut {
  from {
    opacity: 1;
  }
  to {
    opacity: 0;
  }
}

/* 拂晓光芒淡入 */
.fade-in-dawn {
  animation: fadeInDawn var(--duration-dawn-slow) var(--ease-dawn-glow);
}

@keyframes fadeInDawn {
  0% {
    opacity: 0;
    transform: scale(0.95);
  }
  50% {
    opacity: 0.7;
    transform: scale(1.02);
  }
  100% {
    opacity: 1;
    transform: scale(1);
  }
}
```

### 滑动动效

```css
/* 滑动动效 */
.slide-up {
  animation: slideUp var(--duration-dawn-normal) var(--ease-dawn-warm);
}

.slide-down {
  animation: slideDown var(--duration-deep-space-normal) var(--ease-deep-space);
}

.slide-left {
  animation: slideLeft var(--duration-dawn-normal) var(--ease-dawn-warm);
}

.slide-right {
  animation: slideRight var(--duration-deep-space-normal) var(--ease-deep-space);
}

@keyframes slideUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideDown {
  from {
    opacity: 0;
    transform: translateY(-30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

@keyframes slideLeft {
  from {
    opacity: 0;
    transform: translateX(30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

@keyframes slideRight {
  from {
    opacity: 0;
    transform: translateX(-30px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}
```

### 缩放动效

```css
/* 缩放动效 */
.scale-in {
  animation: scaleIn var(--duration-dawn-normal) var(--ease-dawn-bright);
}

.scale-out {
  animation: scaleOut var(--duration-deep-space-fast) var(--ease-deep-space);
}

.scale-bounce {
  animation: scaleBounce var(--duration-dawn-slow) var(--ease-bounce);
}

@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.8);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

@keyframes scaleOut {
  from {
    opacity: 1;
    transform: scale(1);
  }
  to {
    opacity: 0;
    transform: scale(0.8);
  }
}

@keyframes scaleBounce {
  0% {
    opacity: 0;
    transform: scale(0.3);
  }
  50% {
    opacity: 1;
    transform: scale(1.05);
  }
  70% {
    transform: scale(0.9);
  }
  100% {
    opacity: 1;
    transform: scale(1);
  }
}
```

---

## 微交互动效

### 按钮动效

```css
/* 按钮基础动效 */
.btn {
  transition: all var(--duration-dawn-fast) var(--ease-dawn-warm);
  position: relative;
  overflow: hidden;
}

/* 拂晓按钮悬停 */
.btn-dawn:hover {
  transform: translateY(-2px);
  box-shadow: 0 8px 25px rgba(255, 179, 102, 0.3);
}

.btn-dawn:active {
  transform: translateY(0);
  box-shadow: 0 4px 12px rgba(255, 179, 102, 0.2);
}

/* 深空按钮悬停 */
.btn-deep-space:hover {
  transform: translateY(-1px);
  box-shadow: 0 6px 20px rgba(139, 92, 246, 0.25);
}

.btn-deep-space:active {
  transform: translateY(0);
  box-shadow: 0 3px 10px rgba(139, 92, 246, 0.15);
}

/* 按钮波纹效果 */
.btn-ripple {
  position: relative;
}

.btn-ripple::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 50%;
  width: 0;
  height: 0;
  border-radius: 50%;
  background: rgba(255, 255, 255, 0.3);
  transform: translate(-50%, -50%);
  transition: width 0.6s, height 0.6s;
}

.btn-ripple:active::before {
  width: 300px;
  height: 300px;
}
```

### 卡片动效

```css
/* 卡片基础动效 */
.card {
  transition: all var(--duration-dawn-normal) var(--ease-dawn-warm);
}

/* 拂晓卡片悬停 */
.card-dawn:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 0 12px 40px rgba(255, 179, 102, 0.2);
}

/* 深空卡片悬停 */
.card-deep-space:hover {
  transform: translateY(-3px) scale(1.01);
  box-shadow: 0 10px 35px rgba(139, 92, 246, 0.2);
}

/* 卡片进入动效 */
.card-enter {
  animation: cardEnter var(--duration-dawn-slow) var(--ease-dawn-glow);
}

@keyframes cardEnter {
  0% {
    opacity: 0;
    transform: translateY(40px) scale(0.9);
  }
  50% {
    opacity: 0.8;
    transform: translateY(-5px) scale(1.05);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}
```

### 输入框动效

```css
/* 输入框基础动效 */
.input {
  transition: all var(--duration-dawn-fast) var(--ease-dawn-warm);
  position: relative;
}

/* 输入框焦点动效 */
.input:focus {
  transform: translateY(-1px);
  box-shadow: 0 4px 20px rgba(139, 92, 246, 0.15);
}

/* 输入框标签动效 */
.input-label {
  position: absolute;
  top: 50%;
  left: 12px;
  transform: translateY(-50%);
  transition: all var(--duration-dawn-normal) var(--ease-dawn-warm);
  pointer-events: none;
  color: var(--muted-foreground);
}

.input:focus + .input-label,
.input:not(:placeholder-shown) + .input-label {
  top: 0;
  left: 8px;
  font-size: 12px;
  color: var(--primary);
  background: var(--background);
  padding: 0 4px;
}

/* 输入框下划线动效 */
.input-underline {
  position: relative;
}

.input-underline::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 0;
  width: 0;
  height: 2px;
  background: var(--cloudsway-gradient);
  transition: width var(--duration-dawn-normal) var(--ease-dawn-warm);
}

.input-underline:focus-within::after {
  width: 100%;
}
```

---

## 页面转场动效

### 页面进入

```css
/* 页面进入动效 */
.page-enter {
  animation: pageEnter var(--duration-dawn-slow) var(--ease-dawn-glow);
}

@keyframes pageEnter {
  0% {
    opacity: 0;
    transform: translateY(20px) scale(0.98);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* 拂晓页面进入 */
.page-enter-dawn {
  animation: pageEnterDawn var(--duration-dawn-slow) var(--ease-dawn-glow);
}

@keyframes pageEnterDawn {
  0% {
    opacity: 0;
    transform: translateY(30px) scale(0.95);
  }
  50% {
    opacity: 0.8;
    transform: translateY(-5px) scale(1.02);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* 深空页面进入 */
.page-enter-deep-space {
  animation: pageEnterDeepSpace var(--duration-deep-space-slow) var(--ease-deep-space);
}

@keyframes pageEnterDeepSpace {
  0% {
    opacity: 0;
    transform: translateY(20px);
  }
  100% {
    opacity: 1;
    transform: translateY(0);
  }
}
```

### 页面退出

```css
/* 页面退出动效 */
.page-exit {
  animation: pageExit var(--duration-deep-space-fast) var(--ease-deep-space);
}

@keyframes pageExit {
  0% {
    opacity: 1;
    transform: translateY(0);
  }
  100% {
    opacity: 0;
    transform: translateY(-20px);
  }
}

/* 拂晓页面退出 */
.page-exit-dawn {
  animation: pageExitDawn var(--duration-dawn-normal) var(--ease-dawn-warm);
}

@keyframes pageExitDawn {
  0% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
  100% {
    opacity: 0;
    transform: translateY(-30px) scale(0.95);
  }
}
```

---

## 加载动效

### 基础加载

```css
/* 基础加载动效 */
.loading {
  display: inline-block;
  width: 20px;
  height: 20px;
  border: 2px solid var(--border);
  border-radius: 50%;
  border-top-color: var(--primary);
  animation: spin var(--duration-1000) linear infinite;
}

@keyframes spin {
  to {
    transform: rotate(360deg);
  }
}

/* 拂晓加载动效 */
.loading-dawn {
  display: inline-block;
  width: 24px;
  height: 24px;
  border: 3px solid rgba(255, 179, 102, 0.2);
  border-radius: 50%;
  border-top-color: var(--dawn-warm);
  animation: spinDawn var(--duration-1000) var(--ease-dawn-glow) infinite;
}

@keyframes spinDawn {
  0% {
    transform: rotate(0deg) scale(1);
  }
  50% {
    transform: rotate(180deg) scale(1.1);
  }
  100% {
    transform: rotate(360deg) scale(1);
  }
}

/* 深空加载动效 */
.loading-deep-space {
  display: inline-block;
  width: 20px;
  height: 20px;
  border: 2px solid rgba(139, 92, 246, 0.2);
  border-radius: 50%;
  border-top-color: var(--cloudsway-purple);
  animation: spinDeepSpace var(--duration-1000) var(--ease-deep-space) infinite;
}

@keyframes spinDeepSpace {
  to {
    transform: rotate(360deg);
  }
}
```

### 脉冲加载

```css
/* 脉冲加载动效 */
.pulse {
  animation: pulse var(--duration-1000) var(--ease-in-out) infinite;
}

@keyframes pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.5;
  }
}

/* 拂晓脉冲 */
.pulse-dawn {
  animation: pulseDawn var(--duration-1000) var(--ease-dawn-glow) infinite;
}

@keyframes pulseDawn {
  0% {
    opacity: 1;
    transform: scale(1);
  }
  50% {
    opacity: 0.7;
    transform: scale(1.05);
  }
  100% {
    opacity: 1;
    transform: scale(1);
  }
}
```

---

## 消息动效

### 消息发送

```css
/* 消息发送动效 */
.message-sent {
  animation: messageSent var(--duration-dawn-fast) var(--ease-dawn-warm);
}

@keyframes messageSent {
  0% {
    opacity: 0;
    transform: translateY(10px) scale(0.95);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* 用户消息发送 */
.user-message {
  animation: userMessageSent var(--duration-dawn-normal) var(--ease-dawn-bright);
}

@keyframes userMessageSent {
  0% {
    opacity: 0;
    transform: translateY(15px) scale(0.9);
  }
  50% {
    opacity: 0.8;
    transform: translateY(-2px) scale(1.02);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}
```

### 消息接收

```css
/* 消息接收动效 */
.message-received {
  animation: messageReceived var(--duration-dawn-normal) var(--ease-dawn-warm);
}

@keyframes messageReceived {
  0% {
    opacity: 0;
    transform: translateY(20px) scale(0.9);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

/* AI消息接收 */
.ai-message {
  animation: aiMessageReceived var(--duration-dawn-slow) var(--ease-dawn-glow);
}

@keyframes aiMessageReceived {
  0% {
    opacity: 0;
    transform: translateY(25px) scale(0.85);
  }
  30% {
    opacity: 0.6;
    transform: translateY(-3px) scale(1.05);
  }
  60% {
    opacity: 0.9;
    transform: translateY(1px) scale(0.98);
  }
  100% {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}
```

---

## 角色切换动效

### 角色选择器

```css
/* 角色选择器动效 */
.role-selector {
  transition: all var(--duration-dawn-normal) var(--ease-dawn-warm);
}

.role-selector:hover {
  transform: translateY(-2px);
  box-shadow: 0 6px 20px rgba(139, 92, 246, 0.2);
}

.role-selector.active {
  animation: roleActive var(--duration-dawn-slow) var(--ease-dawn-glow);
}

@keyframes roleActive {
  0% {
    transform: scale(1);
  }
  50% {
    transform: scale(1.05);
  }
  100% {
    transform: scale(1);
  }
}

/* 角色切换过渡 */
.role-transition {
  transition: all var(--duration-dawn-normal) var(--ease-dawn-warm);
}

.role-transition.entering {
  opacity: 0;
  transform: translateX(20px);
}

.role-transition.entered {
  opacity: 1;
  transform: translateX(0);
}

.role-transition.exiting {
  opacity: 0;
  transform: translateX(-20px);
}
```

---

## 响应式动效

### 移动端适配

```css
/* 移动端动效优化 */
@media (max-width: 768px) {
  /* 减少移动端动效强度 */
  .btn:hover {
    transform: translateY(-1px);
  }
  
  .card:hover {
    transform: translateY(-2px) scale(1.01);
  }
  
  /* 简化移动端加载动效 */
  .loading-dawn {
    width: 20px;
    height: 20px;
    border-width: 2px;
  }
  
  /* 优化移动端消息动效 */
  .ai-message {
    animation-duration: var(--duration-dawn-normal);
  }
}
```

### 性能优化

```css
/* 性能优化 */
.optimized-animation {
  will-change: transform, opacity;
  transform: translateZ(0);
  backface-visibility: hidden;
}

/* 减少动效偏好 */
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 总结

这个升级版的动效与微交互规范融合了拂晓与深空的设计哲学，为智链平台提供了完整的动效设计系统。通过精心设计的缓动函数、持续时间、基础动效、微交互、页面转场、加载动效和消息动效，确保整个平台具有流畅和专业的用户体验。

**核心特点：**
- 🌅 拂晓光芒：温暖、希望、创新
- 🌌 深空基底：专业、深度、专注
- ⚡ 流畅动效：自然的交互反馈
- 🎯 微交互：精细的用户体验
- 📱 响应式设计：适配各种设备
- ♿ 无障碍支持：包容性设计
- 🚀 性能优化：流畅的用户体验
---
title: "知识域治理待办清单"
owners:
  - LaunchX Knowledge Lab
status: active
last_update: '2025-11-14'
related:
  - 🟣 knowledge/README.md
  - 🟣 knowledge/📖README-知识库总览.md
  - 🟣 knowledge/RULES.md
source: Codex CLI临时整理
impact: high
---

## 背景
基于2025-11-14现状分析：
- **核心结论**：00→03物理治理主干 + Claude Code任务执行系统 = 混合增强架构，两者是层级协作关系，非替代关系
- **关键问题**：YAML冲突、00层级定位混乱、缺乏执行路由表、自动化闭环不完整
- 需形成可交付的 TODO 供 Claude 或知识域维护者批量处理。

## 核心待办
1. **清理文档合并冲突**  
   - 路径：`🟣 knowledge/📖README-知识库总览.md`、`🟣 knowledge/RULES.md`  
   - 行动：移除 `<<<<<<< / ======= / >>>>>>>` 标记、恢复 frontmatter 与正文，并更新 `last_update`。  
   - 风险：若不清理，frontmatter 校验与引用解析均会失败。

2. **确认 00 层目录定位**  
   - 现状 README 仍写 `00_模块引导系统`，实际目录为 `00_待处理信息/`。  
   - 选项：
     - A. 恢复 `00_模块引导系统/` 用于导航，迁移现有指引；
     - B. 继续使用 `00_待处理信息/`，需同步 README/总览中的目录与职责说明。
   - 输出：明确方案 + 对应文档更新。

3. **建立阶段 × 方法论 × Skills 映射**
   - 目的：把 `00→03` 阶段与 `/spec → /plan → /do`、Level S/M/L 方法论、常用 Skills 一次性列清，方便执行端路由。
   - 建议路径：`🟣 knowledge/README.md` 新增章节或在 `todo/` 生成 `knowledge-routing.md` 供讨论。
   - 需附引用：`🟣 knowledge/05_方法论中心/README.md:25`、`🧠 Launch-X Skills生态系统/README.md:33`。

4. **固化巡检与 memory-bank 互链闭环**  
   - 命令：`python3 "🛠️ 系统管理/memory-bank/support_modules/knowledge/tools/knowledge_audit.py"`。  
   - 行动：制定固定步骤（运行脚本 → 记录差异于 `09_周报月报` → Summary 标注“memory-bank 已更新”）。  
   - 需要对接 memory-bank 的“路径对齐零容忍”规则，避免巡检结果悬空。

## 建议执行顺序
1. 先修复冲突，保证文档可解析。  
2. 同步 00 层定位，确保目录和流程一致。  
3. 补映射表，打通执行路由。  
4. 固化巡检闭环，防止治理回退。

## 补充发现的问题

5. **修复引用路径不一致**
   - 问题：`🟣 knowledge/README.md:32` 写 `00_模块引导系统`，但实际目录是 `00_待处理信息/`
   - 影响：导航失效，用户无法找到正确的模块入口
   - 建议：要么重建模块引导目录，要么统一更新文档描述

6. **补充Claude技能映射表**
   - 当前缺失：Level S/M/L与具体Claude Skills的调用关系
   - 需要建立：`00→03阶段` × `业务场景` × `所需Skills`的三维矩阵
   - 参考来源：`🧠 Launch-X Skills生态系统/README.md`中的技能分类

7. **验证自动化工具可用性**
   - 检查：`knowledge_audit.py`脚本是否存在且可执行
   - 验证：memory-bank路径对齐规则是否生效
   - 测试：巡检报告能否正确生成到指定目录

## 校验与交接
- 执行完每项需在 Summary 中记录命令或文件差异，供 Knowledge Lead 审核。
- 若需 Claude 触发脚本或 BMAD 工作流，请在 Summary 中标注"待 Claude：脚本 + 目的 + 风险"。

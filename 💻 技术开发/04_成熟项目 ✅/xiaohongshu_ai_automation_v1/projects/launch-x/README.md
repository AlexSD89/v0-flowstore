# LaunchX 自动化产出目录

- `data/intel/` : Rube 情报 JSON、结构化信号
- `data/drafts/`: Claude 生成的图文草稿与标签
- `assets/generated/`: image-mcp 输出的图片或视频资产

运行 `python automation/run_client.py --client launch-x` 后，以上目录会被自动写入；如需清理，请保留日志副本以便追踪。

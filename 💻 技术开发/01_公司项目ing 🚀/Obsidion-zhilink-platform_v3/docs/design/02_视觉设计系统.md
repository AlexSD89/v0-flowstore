# zhilink-v3 视觉设计系统

## 🎨 设计哲学：Cloudsway 2.0 "拂晓深空"

**设计理念**: 融合温暖与专业的视觉体验  
**核心价值**: 专业 × 科技 × 温暖 × 简洁  
**目标情感**: 信任、高效、创新、人性化  

---

## 🌈 色彩系统

### 主色调定义

```css
:root {
  /* === 核心品牌色彩 === */
  --cloudsway-primary-50: #f8fafc;
  --cloudsway-primary-100: #f1f5f9;
  --cloudsway-primary-200: #e2e8f0;
  --cloudsway-primary-300: #cbd5e1;
  --cloudsway-primary-400: #94a3b8;
  --cloudsway-primary-500: #6366f1;    /* 深邃紫色 - 主品牌色 */
  --cloudsway-primary-600: #4f46e5;
  --cloudsway-primary-700: #4338ca;
  --cloudsway-primary-800: #3730a3;
  --cloudsway-primary-900: #312e81;

  /* === 次要品牌色彩 === */
  --cloudsway-secondary-50: #f0fdfa;
  --cloudsway-secondary-100: #ccfbf1;
  --cloudsway-secondary-200: #99f6e4;
  --cloudsway-secondary-300: #5eead4;
  --cloudsway-secondary-400: #2dd4bf;
  --cloudsway-secondary-500: #06b6d4;    /* 清澈青色 - 次要品牌色 */
  --cloudsway-secondary-600: #0891b2;
  --cloudsway-secondary-700: #0e7490;
  --cloudsway-secondary-800: #155e75;
  --cloudsway-secondary-900: #164e63;

  /* === 强调色彩 === */
  --cloudsway-accent-50: #f5f3ff;
  --cloudsway-accent-100: #ede9fe;
  --cloudsway-accent-200: #ddd6fe;
  --cloudsway-accent-300: #c4b5fd;
  --cloudsway-accent-400: #a78bfa;
  --cloudsway-accent-500: #8b5cf6;      /* 神秘紫罗兰 - 强调色 */
  --cloudsway-accent-600: #7c3aed;
  --cloudsway-accent-700: #6d28d9;
  --cloudsway-accent-800: #5b21b6;
  --cloudsway-accent-900: #4c1d95;

  /* === 深空背景系统 === */
  --cloudsway-background-main: #0f172a;       /* 深空主背景 */
  --cloudsway-background-card: #1e293b;       /* 卡片背景 */
  --cloudsway-background-glass: rgba(30, 41, 59, 0.8);  /* 玻璃态背景 */
  --cloudsway-background-overlay: rgba(15, 23, 42, 0.9); /* 遮罩背景 */

  /* === 文本色彩系统 === */
  --cloudsway-text-primary: #f8fafc;         /* 主要文本 */
  --cloudsway-text-secondary: #cbd5e1;       /* 次要文本 */
  --cloudsway-text-muted: #64748b;           /* 弱化文本 */
  --cloudsway-text-inverse: #0f172a;         /* 反色文本 */

  /* === 边框与分割线 === */
  --cloudsway-border-primary: rgba(203, 213, 225, 0.2);
  --cloudsway-border-secondary: rgba(100, 116, 139, 0.3);
  --cloudsway-border-accent: rgba(139, 92, 246, 0.4);

  /* === 功能性色彩 === */
  --cloudsway-success: #10b981;
  --cloudsway-warning: #f59e0b;
  --cloudsway-error: #ef4444;
  --cloudsway-info: #06b6d4;
}
```

### 6角色专属色彩

```css
:root {
  /* === Alex - 需求理解专家 === */
  --agent-alex-primary: #3b82f6;    /* 蓝色 - 信任与洞察 */
  --agent-alex-bg: rgba(59, 130, 246, 0.1);
  --agent-alex-border: rgba(59, 130, 246, 0.3);

  /* === Sarah - 技术架构师 === */
  --agent-sarah-primary: #8b5cf6;   /* 紫色 - 专业与创新 */
  --agent-sarah-bg: rgba(139, 92, 246, 0.1);
  --agent-sarah-border: rgba(139, 92, 246, 0.3);

  /* === Mike - 体验设计师 === */
  --agent-mike-primary: #10b981;    /* 绿色 - 创意与活力 */
  --agent-mike-bg: rgba(16, 185, 129, 0.1);
  --agent-mike-border: rgba(16, 185, 129, 0.3);

  /* === Emma - 数据分析师 === */
  --agent-emma-primary: #f59e0b;    /* 橙色 - 洞察与智慧 */
  --agent-emma-bg: rgba(245, 158, 11, 0.1);
  --agent-emma-border: rgba(245, 158, 11, 0.3);

  /* === David - 项目管理师 === */
  --agent-david-primary: #6366f1;   /* 靛青 - 执行与秩序 */
  --agent-david-bg: rgba(99, 102, 241, 0.1);
  --agent-david-border: rgba(99, 102, 241, 0.3);

  /* === Catherine - 战略顾问 === */
  --agent-catherine-primary: #ec4899; /* 粉色 - 远见与价值 */
  --agent-catherine-bg: rgba(236, 72, 153, 0.1);
  --agent-catherine-border: rgba(236, 72, 153, 0.3);
}
```

### 渐变色系统

```css
:root {
  /* === 品牌渐变 === */
  --cloudsway-gradient-primary: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
  --cloudsway-gradient-secondary: linear-gradient(135deg, #06b6d4 0%, #10b981 100%);
  --cloudsway-gradient-glass: linear-gradient(135deg, rgba(248, 250, 252, 0.1) 0%, rgba(203, 213, 225, 0.05) 100%);
  
  /* === 背景渐变 === */
  --cloudsway-bg-hero: radial-gradient(ellipse at top, rgba(99, 102, 241, 0.1) 0%, transparent 50%),
                      radial-gradient(ellipse at bottom, rgba(6, 182, 212, 0.1) 0%, transparent 50%),
                      #0f172a;
  
  /* === 6角色协作渐变 === */
  --agents-collaboration-gradient: conic-gradient(
    from 0deg,
    #3b82f6 0deg 60deg,
    #8b5cf6 60deg 120deg,
    #10b981 120deg 180deg,
    #f59e0b 180deg 240deg,
    #6366f1 240deg 300deg,
    #ec4899 300deg 360deg
  );
}
```

---

## 🔤 字体系统

### 字体族定义

```css
:root {
  /* === 主要字体 === */
  --cloudsway-font-sans: 'Inter', 'PingFang SC', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
  --cloudsway-font-serif: 'Inter Tight', 'PingFang SC', serif;
  --cloudsway-font-mono: 'JetBrains Mono', 'SF Mono', 'Cascadia Code', Consolas, monospace;
  --cloudsway-font-display: 'Orbitron', 'Inter Tight', sans-serif; /* 科技感标题字体 */
  --cloudsway-font-numeric: 'JetBrains Mono', monospace; /* 数字专用字体 */
}
```

### 字体尺寸与行高

```css
:root {
  /* === 字体尺寸系统 === */
  --text-xs: 0.75rem;     /* 12px */
  --text-sm: 0.875rem;    /* 14px */
  --text-base: 1rem;      /* 16px */
  --text-lg: 1.125rem;    /* 18px */
  --text-xl: 1.25rem;     /* 20px */
  --text-2xl: 1.5rem;     /* 24px */
  --text-3xl: 1.875rem;   /* 30px */
  --text-4xl: 2.25rem;    /* 36px */
  --text-5xl: 3rem;       /* 48px */
  --text-6xl: 3.75rem;    /* 60px */

  /* === 行高系统 === */
  --leading-none: 1;
  --leading-tight: 1.25;
  --leading-snug: 1.375;
  --leading-normal: 1.5;
  --leading-relaxed: 1.625;
  --leading-loose: 2;
}
```

### 字体权重

```css
:root {
  --font-light: 300;
  --font-normal: 400;
  --font-medium: 500;
  --font-semibold: 600;
  --font-bold: 700;
  --font-extrabold: 800;
  --font-black: 900;
}
```

### 字体应用规范

```typescript
// 字体使用场景定义
const TYPOGRAPHY_USAGE = {
  display: {
    family: 'var(--cloudsway-font-display)',
    weight: 'var(--font-bold)',
    use: ['Hero标题', '品牌标识', '关键数据展示']
  },
  
  heading: {
    family: 'var(--cloudsway-font-serif)',
    weight: 'var(--font-semibold)',
    use: ['页面标题', '章节标题', '卡片标题']
  },
  
  body: {
    family: 'var(--cloudsway-font-sans)',
    weight: 'var(--font-normal)',
    use: ['正文内容', '描述文本', '用户输入']
  },
  
  numeric: {
    family: 'var(--cloudsway-font-numeric)',
    weight: 'var(--font-medium)',
    use: ['价格显示', '统计数据', '进度百分比']
  },
  
  code: {
    family: 'var(--cloudsway-font-mono)',
    weight: 'var(--font-normal)',
    use: ['代码片段', 'API接口', '技术参数']
  }
} as const;
```

---

## 📐 间距系统

### 空间尺度

```css
:root {
  /* === 基础间距单位 === */
  --space-0: 0;
  --space-px: 1px;
  --space-0.5: 0.125rem;  /* 2px */
  --space-1: 0.25rem;     /* 4px */
  --space-1.5: 0.375rem;  /* 6px */
  --space-2: 0.5rem;      /* 8px */
  --space-2.5: 0.625rem;  /* 10px */
  --space-3: 0.75rem;     /* 12px */
  --space-3.5: 0.875rem;  /* 14px */
  --space-4: 1rem;        /* 16px */
  --space-5: 1.25rem;     /* 20px */
  --space-6: 1.5rem;      /* 24px */
  --space-7: 1.75rem;     /* 28px */
  --space-8: 2rem;        /* 32px */
  --space-9: 2.25rem;     /* 36px */
  --space-10: 2.5rem;     /* 40px */
  --space-11: 2.75rem;    /* 44px */
  --space-12: 3rem;       /* 48px */
  --space-14: 3.5rem;     /* 56px */
  --space-16: 4rem;       /* 64px */
  --space-20: 5rem;       /* 80px */
  --space-24: 6rem;       /* 96px */
  --space-28: 7rem;       /* 112px */
  --space-32: 8rem;       /* 128px */
}
```

### 黄金比例布局系统

```css
:root {
  /* === 黄金比例 φ = 1.618 === */
  --ratio-phi: 1.618;
  --ratio-phi-inv: 0.618; /* 1/φ */

  /* === 布局比例应用 === */
  --layout-sidebar-width: 38.2%;     /* φ⁻¹ × 100% */
  --layout-content-width: 61.8%;     /* φ⁻² × 100% */
  
  /* === 三栏布局 (1:3:6) === */
  --layout-nav-width: 10%;           /* 导航栏 */
  --layout-side-width: 30%;          /* 侧边栏 */
  --layout-main-width: 60%;          /* 主内容区 */

  /* === 垂直节奏 === */
  --rhythm-base: 1.5rem;             /* 24px 基础行高 */
  --rhythm-small: 0.9375rem;         /* 15px = 24px ÷ φ */
  --rhythm-large: 2.4375rem;         /* 39px = 24px × φ */
}
```

---

## 🎭 组件样式系统

### 卡片组件样式

```css
/* === 基础卡片样式 === */
.card-cloudsway {
  background: var(--cloudsway-background-card);
  border: 1px solid var(--cloudsway-border-primary);
  border-radius: 1rem;
  padding: 1.5rem;
  box-shadow: 
    0 4px 6px -1px rgba(0, 0, 0, 0.1),
    0 2px 4px -1px rgba(0, 0, 0, 0.06);
  backdrop-filter: blur(12px);
  transition: all 0.3s cubic-bezier(0.4, 0.0, 0.2, 1);
}

.card-cloudsway:hover {
  box-shadow: 
    0 20px 25px -5px rgba(0, 0, 0, 0.1),
    0 10px 10px -5px rgba(0, 0, 0, 0.04);
  transform: translateY(-2px);
  border-color: var(--cloudsway-border-accent);
}

/* === 玻璃态效果增强 === */
.card-glass {
  background: var(--cloudsway-background-glass);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
  border: 1px solid rgba(255, 255, 255, 0.1);
  box-shadow: 
    0 8px 32px rgba(0, 0, 0, 0.12),
    inset 0 1px 0 rgba(255, 255, 255, 0.1),
    inset 0 -1px 0 rgba(0, 0, 0, 0.1);
}

/* === 6角色专属卡片样式 === */
.agent-card-alex {
  border-left: 4px solid var(--agent-alex-primary);
  background: linear-gradient(135deg, var(--agent-alex-bg) 0%, transparent 100%);
}

.agent-card-sarah {
  border-left: 4px solid var(--agent-sarah-primary);
  background: linear-gradient(135deg, var(--agent-sarah-bg) 0%, transparent 100%);
}

.agent-card-mike {
  border-left: 4px solid var(--agent-mike-primary);
  background: linear-gradient(135deg, var(--agent-mike-bg) 0%, transparent 100%);
}

.agent-card-emma {
  border-left: 4px solid var(--agent-emma-primary);
  background: linear-gradient(135deg, var(--agent-emma-bg) 0%, transparent 100%);
}

.agent-card-david {
  border-left: 4px solid var(--agent-david-primary);
  background: linear-gradient(135deg, var(--agent-david-bg) 0%, transparent 100%);
}

.agent-card-catherine {
  border-left: 4px solid var(--agent-catherine-primary);
  background: linear-gradient(135deg, var(--agent-catherine-bg) 0%, transparent 100%);
}
```

### 按钮系统样式

```css
/* === 基础按钮样式 === */
.btn-cloudsway {
  font-family: var(--cloudsway-font-sans);
  font-weight: var(--font-medium);
  border-radius: 0.75rem;
  transition: all 0.2s cubic-bezier(0.4, 0.0, 0.2, 1);
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 0.5rem;
  border: none;
  outline: none;
  position: relative;
  overflow: hidden;
}

/* === 按钮尺寸 === */
.btn-sm {
  padding: 0.5rem 1rem;
  font-size: var(--text-sm);
  min-height: 2.5rem;
}

.btn-md {
  padding: 0.75rem 1.5rem;
  font-size: var(--text-base);
  min-height: 3rem;
}

.btn-lg {
  padding: 1rem 2rem;
  font-size: var(--text-lg);
  min-height: 3.5rem;
}

.btn-xl {
  padding: 1.25rem 2.5rem;
  font-size: var(--text-xl);
  min-height: 4rem;
}

/* === 按钮变体 === */
.btn-primary {
  background: var(--cloudsway-gradient-primary);
  color: white;
  box-shadow: 0 4px 14px rgba(99, 102, 241, 0.25);
}

.btn-primary:hover {
  box-shadow: 0 6px 20px rgba(99, 102, 241, 0.4);
  transform: translateY(-1px);
}

.btn-secondary {
  background: var(--cloudsway-background-card);
  color: var(--cloudsway-text-primary);
  border: 1px solid var(--cloudsway-border-primary);
}

.btn-secondary:hover {
  background: var(--cloudsway-background-glass);
  border-color: var(--cloudsway-border-accent);
}

.btn-ghost {
  background: transparent;
  color: var(--cloudsway-text-secondary);
  border: 1px solid var(--cloudsway-border-primary);
}

.btn-ghost:hover {
  background: var(--cloudsway-background-glass);
  color: var(--cloudsway-text-primary);
}

/* === 按钮状态 === */
.btn-cloudsway:disabled {
  opacity: 0.6;
  cursor: not-allowed;
  transform: none !important;
  box-shadow: none !important;
}

.btn-cloudsway:active {
  transform: scale(0.98);
}
```

### 表单组件样式

```css
/* === 输入框样式 === */
.input-cloudsway {
  width: 100%;
  padding: 0.75rem 1rem;
  border: 1px solid var(--cloudsway-border-primary);
  border-radius: 0.75rem;
  background: var(--cloudsway-background-glass);
  color: var(--cloudsway-text-primary);
  font-family: var(--cloudsway-font-sans);
  font-size: var(--text-base);
  transition: all 0.2s cubic-bezier(0.4, 0.0, 0.2, 1);
  backdrop-filter: blur(12px);
}

.input-cloudsway:focus {
  outline: none;
  border-color: var(--cloudsway-primary-500);
  box-shadow: 
    0 0 0 3px rgba(99, 102, 241, 0.1),
    0 4px 14px rgba(99, 102, 241, 0.15);
  background: var(--cloudsway-background-card);
}

.input-cloudsway::placeholder {
  color: var(--cloudsway-text-muted);
}

/* === 文本域样式 === */
.textarea-cloudsway {
  resize: vertical;
  min-height: 120px;
  line-height: var(--leading-relaxed);
}

/* === 选择框样式 === */
.select-cloudsway {
  appearance: none;
  background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%236b7280' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='m6 8 4 4 4-4'/%3e%3c/svg%3e");
  background-position: right 0.75rem center;
  background-repeat: no-repeat;
  background-size: 1.5em 1.5em;
  padding-right: 3rem;
}
```

---

## 🎯 6角色视觉身份系统

### 角色头像设计规范

```typescript
// 6角色头像设计系统
interface AgentAvatarSystem {
  alex: {
    primaryColor: '#3b82f6';
    icon: '🎯' | 'brain';
    personality: '温和智慧的顾问形象';
    visualStyle: {
      backgroundColor: 'linear-gradient(135deg, #3b82f6, #1e40af)',
      borderColor: '#3b82f6',
      iconColor: 'white',
      glowEffect: '0 0 20px rgba(59, 130, 246, 0.3)'
    }
  },
  
  sarah: {
    primaryColor: '#8b5cf6';
    icon: '⚡' | 'zap';
    personality: '专业严谨的技术专家';
    visualStyle: {
      backgroundColor: 'linear-gradient(135deg, #8b5cf6, #7c3aed)',
      borderColor: '#8b5cf6',
      iconColor: 'white',
      glowEffect: '0 0 20px rgba(139, 92, 246, 0.3)'
    }
  },
  
  mike: {
    primaryColor: '#10b981';
    icon: '🎨' | 'palette';
    personality: '创意活跃的设计师';
    visualStyle: {
      backgroundColor: 'linear-gradient(135deg, #10b981, #059669)',
      borderColor: '#10b981',
      iconColor: 'white',
      glowEffect: '0 0 20px rgba(16, 185, 129, 0.3)'
    }
  },
  
  emma: {
    primaryColor: '#f59e0b';
    icon: '📊' | 'trending-up';
    personality: '理性分析的数据专家';
    visualStyle: {
      backgroundColor: 'linear-gradient(135deg, #f59e0b, #d97706)',
      borderColor: '#f59e0b',
      iconColor: 'white',
      glowEffect: '0 0 20px rgba(245, 158, 11, 0.3)'
    }
  },
  
  david: {
    primaryColor: '#6366f1';
    icon: '📋' | 'check-circle';
    personality: '高效有序的管理者';
    visualStyle: {
      backgroundColor: 'linear-gradient(135deg, #6366f1, #4f46e5)',
      borderColor: '#6366f1',
      iconColor: 'white',
      glowEffect: '0 0 20px rgba(99, 102, 241, 0.3)'
    }
  },
  
  catherine: {
    primaryColor: '#ec4899';
    icon: '🌟' | 'target';
    personality: '高瞻远瞩的战略家';
    visualStyle: {
      backgroundColor: 'linear-gradient(135deg, #ec4899, #db2777)',
      borderColor: '#ec4899',
      iconColor: 'white',
      glowEffect: '0 0 20px rgba(236, 72, 153, 0.3)'
    }
  }
}
```

### 角色协作可视化

```css
/* === 协作进度环形图 === */
.collaboration-ring {
  width: 200px;
  height: 200px;
  border-radius: 50%;
  background: var(--agents-collaboration-gradient);
  position: relative;
  animation: rotate 60s linear infinite;
}

.collaboration-ring::before {
  content: '';
  position: absolute;
  top: 10px;
  left: 10px;
  right: 10px;
  bottom: 10px;
  background: var(--cloudsway-background-card);
  border-radius: 50%;
  backdrop-filter: blur(20px);
}

@keyframes rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* === 角色状态指示器 === */
.agent-status-indicator {
  width: 12px;
  height: 12px;
  border-radius: 50%;
  position: absolute;
  animation: pulse 2s ease-in-out infinite;
}

.agent-status-thinking {
  background: #f59e0b;
  box-shadow: 0 0 15px rgba(245, 158, 11, 0.6);
}

.agent-status-active {
  background: #10b981;
  box-shadow: 0 0 15px rgba(16, 185, 129, 0.6);
}

.agent-status-complete {
  background: #6366f1;
  box-shadow: 0 0 15px rgba(99, 102, 241, 0.6);
}

@keyframes pulse {
  0%, 100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.2); opacity: 0.7; }
}
```

---

## 📱 响应式设计系统

### 断点系统

```css
:root {
  /* === 响应式断点 === */
  --breakpoint-sm: 640px;    /* 小屏设备 */
  --breakpoint-md: 768px;    /* 平板设备 */
  --breakpoint-lg: 1024px;   /* 笔记本 */
  --breakpoint-xl: 1280px;   /* 桌面 */
  --breakpoint-2xl: 1536px;  /* 大屏桌面 */
}

/* === 响应式工具类 === */
@media (max-width: 768px) {
  .card-cloudsway {
    padding: 1rem;
    border-radius: 0.75rem;
  }
  
  .btn-cloudsway {
    min-height: 44px; /* 移动端触摸标准 */
    padding: 0.875rem 1.25rem;
  }
  
  /* 移动端字体调整 */
  .text-responsive-sm { font-size: clamp(0.875rem, 2.5vw, 1rem); }
  .text-responsive-md { font-size: clamp(1rem, 3vw, 1.125rem); }
  .text-responsive-lg { font-size: clamp(1.125rem, 4vw, 1.5rem); }
  .text-responsive-xl { font-size: clamp(1.5rem, 5vw, 2.25rem); }
}

/* === 移动端适配增强 === */
@media (max-width: 640px) {
  :root {
    --space-section: var(--space-16); /* 移动端章节间距减小 */
  }
  
  .container-mobile {
    padding-left: 1rem;
    padding-right: 1rem;
  }
  
  /* 6角色卡片移动端堆叠 */
  .agents-grid {
    grid-template-columns: 1fr;
    gap: 1rem;
  }
  
  .agent-card {
    padding: 1rem;
  }
}
```

---

## ♿ 无障碍设计规范

### 色彩对比度

```css
/* === WCAG AA级对比度标准 === */
:root {
  /* 确保所有文本颜色达到4.5:1的对比度 */
  --text-aa-contrast-primary: #ffffff;   /* 对深色背景 21:1 */
  --text-aa-contrast-secondary: #e2e8f0; /* 对深色背景 12.6:1 */
  --text-aa-contrast-muted: #94a3b8;     /* 对深色背景 6.4:1 */
}

/* === 高对比度模式支持 === */
@media (prefers-contrast: high) {
  :root {
    --cloudsway-border-primary: rgba(255, 255, 255, 0.6);
    --cloudsway-text-muted: #cbd5e1;
  }
  
  .card-cloudsway {
    border-width: 2px;
  }
  
  .btn-cloudsway {
    border-width: 2px;
    font-weight: var(--font-semibold);
  }
}

/* === 动效减少偏好支持 === */
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

### 焦点管理

```css
/* === 焦点样式系统 === */
.focus-cloudsway:focus {
  outline: 2px solid var(--cloudsway-primary-500);
  outline-offset: 2px;
  border-radius: 0.375rem;
}

.focus-visible-cloudsway:focus-visible {
  outline: 2px solid var(--cloudsway-accent-500);
  outline-offset: 4px;
  box-shadow: 0 0 0 4px rgba(139, 92, 246, 0.2);
}

/* === 跳转链接 === */
.skip-link {
  position: absolute;
  top: -40px;
  left: 6px;
  background: var(--cloudsway-primary-500);
  color: white;
  padding: 8px;
  border-radius: 4px;
  text-decoration: none;
  z-index: 1000;
  transition: top 0.3s;
}

.skip-link:focus {
  top: 6px;
}
```

---

## 🌙 暗色模式适配

```css
/* === 深色主题变量覆盖 === */
[data-theme="dark"] {
  /* 背景色调整 */
  --cloudsway-background-main: #030712;
  --cloudsway-background-card: #111827;
  --cloudsway-background-glass: rgba(17, 24, 39, 0.8);
  
  /* 文本色调整 */
  --cloudsway-text-primary: #f9fafb;
  --cloudsway-text-secondary: #d1d5db;
  --cloudsway-text-muted: #9ca3af;
  
  /* 边框色调整 */
  --cloudsway-border-primary: rgba(209, 213, 219, 0.1);
  --cloudsway-border-secondary: rgba(156, 163, 175, 0.2);
}

/* === 亮色主题 === */
[data-theme="light"] {
  --cloudsway-background-main: #ffffff;
  --cloudsway-background-card: #f8fafc;
  --cloudsway-background-glass: rgba(248, 250, 252, 0.8);
  
  --cloudsway-text-primary: #0f172a;
  --cloudsway-text-secondary: #475569;
  --cloudsway-text-muted: #64748b;
  
  --cloudsway-border-primary: rgba(15, 23, 42, 0.1);
  --cloudsway-border-secondary: rgba(71, 85, 105, 0.2);
}

/* === 系统主题偏好检测 === */
@media (prefers-color-scheme: dark) {
  :root {
    color-scheme: dark;
  }
}

@media (prefers-color-scheme: light) {
  :root {
    color-scheme: light;
  }
}
```

---

## 🎨 设计Token导出

```typescript
// 设计系统Token
export const DESIGN_TOKENS = {
  colors: {
    primary: {
      50: '#f8fafc',
      500: '#6366f1',
      900: '#312e81'
    },
    // ... 完整色彩定义
  },
  
  spacing: {
    0: '0',
    px: '1px',
    0.5: '0.125rem',
    // ... 完整间距定义
  },
  
  typography: {
    fontFamily: {
      sans: ['Inter', 'sans-serif'],
      display: ['Orbitron', 'sans-serif'],
      // ... 完整字体定义
    },
    fontSize: {
      xs: '0.75rem',
      base: '1rem',
      // ... 完整尺寸定义
    }
  },
  
  borderRadius: {
    none: '0',
    sm: '0.375rem',
    md: '0.75rem',
    lg: '1rem',
    full: '9999px'
  },
  
  boxShadow: {
    sm: '0 1px 2px rgba(0, 0, 0, 0.05)',
    md: '0 4px 6px -1px rgba(0, 0, 0, 0.1)',
    lg: '0 10px 15px -3px rgba(0, 0, 0, 0.1)',
    // ... 完整阴影定义
  }
} as const;
```

---

## 🎯 验收标准

### 视觉一致性检查
- [ ] 所有组件遵循Cloudsway 2.0设计系统
- [ ] 6角色色彩应用正确且一致
- [ ] 字体层级清晰，可读性良好
- [ ] 间距系统符合黄金比例

### 响应式适配检查
- [ ] 5个断点下布局正常
- [ ] 移动端触摸目标≥44px
- [ ] 文本在所有屏幕尺寸下可读
- [ ] 图片和媒体内容自适应

### 无障碍合规检查
- [ ] 色彩对比度≥4.5:1 (WCAG AA)
- [ ] 键盘导航完整可用
- [ ] Screen Reader兼容
- [ ] 焦点管理清晰可见

### 性能优化检查
- [ ] CSS文件压缩优化
- [ ] 字体文件懒加载
- [ ] 动画性能60fps
- [ ] 图片格式现代化(WebP/AVIF)

---

**文档版本**: 1.0.0  
**最后更新**: 2025年8月13日  
**设计团队**: zhilink-v3 UI/UX团队